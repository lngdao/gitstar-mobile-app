# Environment Configuration
# For production builds, manage these via EAS Dashboard instead

# App Variant (development | preview | production)
# Controls app name, bundle ID, and other variant-specific settings
# Default: development
APP_VARIANT=development

# API Configuration
EXPO_PUBLIC_API_URL=https://api.example.com

# Feature Flags
EXPO_PUBLIC_ENABLE_DEBUG=true
