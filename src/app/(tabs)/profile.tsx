import { Box, Text } from '@/shared/components';

export default function ProfileScreen() {
  return (
    <Box bg="primary" className="flex-1 items-center justify-center">
      <Text variant="heading" color="primary">Profile</Text>
    </Box>
  );
}
